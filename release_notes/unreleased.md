**Unreleased**

* Enabled Tenable.sc server certificate verification by default while preserving an explicit opt-out.
* Bounded vulnerability pagination using the server-declared total and a hard safety ceiling.
